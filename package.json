{
  "name": "opencode-timeline-plugin",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Conversation history node viewer for the OpenCode TUI (sidebar timeline)",
  "exports": {
    "./tui": {
      "import": "./src/panel.tsx"
    }
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "echo 'TODO: add build' && exit 1"
  },
  "dependencies": {
    "@opencode-ai/plugin": "^1.18.30",
    "@opentui/core": "^0.4.5",
    "@opentui/solid": "^0.4.5",
    "solid-js": "1.9.12"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  },
  "engines": {
    "bun": ">=1.0",
    "opencode": "^1.0.0"
  }
}
